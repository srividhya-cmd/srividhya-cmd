<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple SVG Example</title>
    <style>
        /* Optional: Add some styles to center the SVG and give it a border */
        .svg-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            border: 2px solid #ddd;
            background-color: #f0f0f0;
        }
        svg {
            background-color: #fff;
        }
    </style>
</head>
<body>
    <div class="svg-container">
        <svg width="200" height="200" xmlns="http://www.w3.org/2000/svg">
            <!-- Draw a rectangle -->
            <rect x="10" y="10" width="100" height="50" fill="lightblue" stroke="black" stroke-width="2" />
            
            <!-- Draw a circle -->
            <circle cx="150" cy="50" r="30" fill="lightgreen" stroke="black" stroke-width="2" />
            
            <!-- Draw a line -->
            <line x1="10" y1="100" x2="190" y2="100" stroke="orange" stroke-width="4" />
            
            <!-- Add some text -->
            <text x="10" y="150" font-family="Arial" font-size="16" fill="black">SVG Example</text>
        </svg>
    </div>
</body>
</html>
